namespace soru3;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Karakter girin: ");
        string caracter=Console.Readline();

        if (a && A && e && E && u && U && ü && Ü && ı && I && i && İ && o && O && ö && ö)
        
        {
            Console.WriteLine("Girilen karakter sesli harftir.");
        }
        else

        {
            Console.WriteLine("Girilen karakter sesli harf değildir!");
        }

        else
        
        {
            Console.WriteLine("Lütfen geçerli karakter girin");
        }
        

        
    }
}
